#include<stdio.h>
int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    if(b != 0){
        if(a%b == 0){
            printf("divisible");
        }
        else{
            printf("error not divisible");
        }
    }
    else{
        printf("divistion by 0 is not allowed");
    }
    
}
