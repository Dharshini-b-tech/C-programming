#include <stdio.h>

int main() {
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);
    if ((a ^ b) == 0) {
        printf("Both numbers are equal\n");
    } else {
        printf("Both numbers are not equal\n");
    }

    return 0;
}
