#include <stdio.h>

int main() {
   int totChoc,perDay,pDays,totNeed;
   scanf("%d %d %d",&totChoc,&perDay,&pDays);
   totNeed=perDay*pDays;
   
   if(totChoc>totNeed){
       printf("1");
   }
   else{
       printf("0");
   }

    return 0;
}
