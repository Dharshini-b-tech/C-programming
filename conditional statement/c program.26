#include <stdio.h>

int main() {
    int a,b,c,result;
    scanf("%d %d",&a,&b);
    c=a*b;
    if(c%2==0){
        result=c/2;
        printf("%d",result);
    }
    else{
        printf("invalid");
    }
    

    return 0;
}
